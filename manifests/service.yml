apiVersion: v1
kind: Service
metadata:
    name: stvansolanobikesharingsampleapp
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: stvansolanobikesharingsampleapp